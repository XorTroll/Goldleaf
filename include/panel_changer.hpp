#pragma once

#include "menu.hpp"

namespace menu
{
    namespace main_menu
    {
        void titleInfoSelected(menu::Menu *menu);
        void exitSelected(menu::Menu *menu);
    }

    namespace tinfo_menu
    {
        
    }
}